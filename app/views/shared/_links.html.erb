<!-- These links are for inside the controller -->
<% if controller.controller_name == nil or controller.controller_name == 'activity' or controller.controller_name == '' %>
  <!-- Leave links for the activity controller here -->
  <p class="link"><%= link_to 'Log New Activity', {:action => 'new', :controller => 'activity'} %></p>
<% end %>
<% if controller.controller_name == 'activity_type' %>
  <!-- Leave links for the activity type controller -->
  <p class="link"><%= link_to 'Register New Activity Type', {:action => 'new', :controller => 'activity_type'} %></p>
<% end %>
<% if controller.controller_name == 'group' %>
  <!-- Leave links for the group controller here -->
  <p class="link"><%= link_to 'Create A New Group', {:action => 'new', :controller => 'group'} %></p>
<% end %>

<!-- These links are to other controllers-->
<% if controller.controller_name != nil and controller.controller_name != "activity" %>
  <p class="link"><%= link_to 'View Activity Log', {:action => 'index', :controller => 'activity'}%></p>
  <br>
<% end %>
<% if controller.controller_name != "activity_type" %>
  <p class='link'><%= link_to "View Activity Types", {:action => 'index', :controller => 'activity_type'}%></p>
  <br>
<% end %>
<% if controller.controller_name != "group" %>
  <p class='link'><%= link_to "View Groups", {:action => 'index', :controller => 'group'}%></p>
<% end %>
<br>