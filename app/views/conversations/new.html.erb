<br>
<br>
<% if not @user%>
<h1>New Conversation</h1>
<% end %>
<% if @user%>
<h1>New Conversation with <%=@user.full_name%></h1>
<% end %>

<%= form_tag conversations_path, method: :post do %>

    <% if not @user%>
    <%#<% if @user.id != current_user.id %>
        <div>
            <%= select_tag :recipient_id, options_from_collection_for_select(@recipients, :id, :full_name)%>
        </div>
        
        <div>
            <%= text_field_tag :subject, nil, placeholder: "Subject" %>
        </div>
    
        <div>
            <%= text_area_tag :body, nil, placeholder: "Whats on your mind?" %>
        </div>
    <% end %>
    <% if @user%>
        <div>
            <%= hidden_field_tag(:recipient_id, "#{@user.id}") %>
        </div>
        
        <div>
            <%= text_field_tag :subject, nil, placeholder: "Subject" %>
        </div>
    
        <div>
            <%= text_area_tag :body, nil, placeholder: "Whats on your mind?" %>
        </div>
        
    <% end %>
    
    <%= submit_tag %>
    <% end %>